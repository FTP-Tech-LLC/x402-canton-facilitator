{
  "name": "canton-x402-facilitator",
  "private": true,
  "description": "Canton x402 facilitator: /verify + /settle service for the exact scheme on Canton Network (transfer-factory, sponsored gas)",
  "license": "Apache-2.0",
  "packageManager": "pnpm@9.12.0",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "eslint": "^9",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8",
    "vitest": "^2.1.8"
  }
}
